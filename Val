<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Mensaje</title>
  <style>
    body{font-family:Arial; display:grid; place-items:center; min-height:100vh; margin:0; background:#f6f6ff;}
    .card{background:white; padding:24px; border-radius:18px; box-shadow:0 10px 30px rgba(0,0,0,.08); max-width:520px; width:90%; text-align:center;}
    h1{margin:0 0 10px;}
    p{font-size:18px; margin:0 0 18px;}
    .hint{opacity:.65; font-size:13px;}
  </style>
</head>
<body>
  <div class="card">
    <h1 id="titulo">✨ Hola ✨</h1>
    <p id="mensaje">Este mensaje es para ti mi amor 💖</p>
    <div class="hint">Tip: prueba agregando <b>?m=</b> en el link</div>
  </div>

  <script>
    // Lee el mensaje desde la URL: ?m=Tu%20mensaje
    const params = new URLSearchParams(location.search);
    const m = params.get("m");
    const t = params.get("t");

    if (t) document.getElementById("titulo").textContent = t;
    if (m) document.getElementById("mensaje").textContent = m;
  </script>
</body>
</html>
